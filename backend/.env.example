# Backend Server Configuration
PORT=3000

# Google Cloud Translation API Key (Optional - for multilingual translation)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Make sure to enable the Cloud Translation API first
GOOGLE_TRANSLATE_API_KEY=your_google_translate_api_key_here

# Note: Document embeddings now use @xenova/transformers (local, no API key needed)
# The system runs completely locally without requiring external API keys for vector search

